version: "3.9"

networks:
  backend:
    ipam:
      driver: default
      config:
        - subnet: 192.10.0.0/16

services:
  redis:
    image: redis:alpine
    container_name: redis_db
    ports:
      - 6379:6379
    networks:
      backend:
        ipv4_address: 192.10.1.5

